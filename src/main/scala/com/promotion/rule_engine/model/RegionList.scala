package com.promotion.rule_engine.model

case class RegionList(countries: List[String],
					  states: List[String],
					  cities: List[String],
					  areas: List[String],
					  pincodes: List[String])
 